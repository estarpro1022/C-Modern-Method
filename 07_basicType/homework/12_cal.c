#include <stdio.h>

int main() {
    double a, b;
    double sum;
    char ch;

    printf("Enter an expression:");
    scanf("%lf%c%lf", &a, &ch, &b);
    while (1) {
        scanf("%d%c")
        break;
    }
}

